@model GolaStripe.Models.ReceiptVm
@{
    ViewBag.Title = Strings.Receipt_Title;
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@Html.Partial("_ReceiptStyles")

<div class="receipt-wrap">
@if (!string.IsNullOrEmpty(ViewBag.Error as string))
{
    <p class="err">@ViewBag.Error</p>
    if (!string.IsNullOrEmpty(ViewBag.ErrorDetail as string))
    {
        <p class="hint">@ViewBag.ErrorDetail</p>
    }
    <p class="no-print"><a class="btn secondary" href="@DeviceAuth.PublicRedirectUrl">@Strings.Nav_Back</a></p>
}
else if (Model == null)
{
    <p class="err">@Strings.Success_MissingSession</p>
    <p class="no-print"><a class="btn secondary" href="@DeviceAuth.PublicRedirectUrl">@Strings.Nav_Back</a></p>
}
else
{
    @Html.Partial("_Receipt", Model)

    <div class="actions no-print">
        <button type="button" class="btn" onclick="window.print()">@Strings.Receipt_SavePdf</button>
        <a class="btn secondary" href="@DeviceAuth.PublicRedirectUrl">@Strings.Nav_BackHome</a>
        <p class="hint">@Strings.Receipt_PrintHint</p>
    </div>
}
</div>
